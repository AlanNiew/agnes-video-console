#!/bin/sh
# 分支路径护栏：按当前分支校验暂存文件（规则与启用方式见 docs/BRANCHING.md）
# 启用（每个克隆一次）：git config core.hooksPath .githooks
# 注意：护栏脚本只存在于含本规范的平台分支上；旧分支（如 feature/dreamina-integration）没有它时跳过，不阻断提交。
if [ -f tools/branch-guard.js ]; then
  node tools/branch-guard.js || exit 1
fi
