# 平台改进清单（Improvement Backlog）

> **来源**：《幻灯屋》系列 S1E01–E02 实战复盘（2026-09，共 26 镜全流程）。
> **用法**：按优先级取用；每条含「问题 / 证据 / 影响 / 建议」四要素，可直接转 issue。
> **状态**：⬜ 待办 · 🟡 部分具备 · ✅ 已落地（见附录 A）

---

## P0 — 解锁"系列化"（不补则剧本被功能反向限制）

### ✅ P0-1 多角色引用（一个项目只能锚一张角色图）—— 已落地 v2.5

- **落地方案**：镜头级 `ref_image_ids`（≤5，省略=全部定稿角色图）+ 定稿改 `append`（追加）+ `pipeline` 多图注入。
- **问题**（原）：视频提交只注入一张角色定稿图，多角色同框/对手戏无法锚定。
- **证据**（原）：`services/pipeline.js` `submitVideoTask()` 取 `projects.selectedImage(p.id, 'character')`，
  且 `selectImage` 语义为"同一 kind 只有一张 selected"。
- **影响**：E02 的"风花＋修鞋匠"只能靠**构图规避**（鞋匠不给正脸/只出镜手部与侧影）+ 文字锚硬撑。
- **实现**：
  1. `select-image` 支持 `append:true`（追加定稿）/ `selected:false`（取消定稿），默认仍为替换（向后兼容）；
  2. 镜头 `ref_image_ids`（本镜出场角色图 id，≤5）——`POST/PATCH /api/projects/:id/shots/:shotId`；
  3. `pipeline` 按镜头选角色，注入多图（Flash 上限 5 张）；前缀自动并列编号，已含 `<Picture N>` 幂等不改写；
  4. 图片任务"首张自动定稿"改为**仅当该 kind 尚无定稿时**（避免多角色下历史图累积）；
  5. 数据层 `shots.ref_image_ids` 迁移 + `projects.selectedImages()` + `imageRowToApi` 统一映射。
- **验证**：e2e 新增多角色用例（多张定稿/取消/精确选取/前缀）+ 单测 99；**实拍**：镜12 绑两张参考图
  生成「風花＋修鞋匠」同框，两人形象与各自种子图一致 ✓。

### ✅ P0-2 跨项目角色资产库（系列的核心资产没有一等公民）—— 已落地 v2.5

- **落地方案**：`settings.character_library`（KV 零新表）+ 收藏/列表/删除 + `POST /api/projects/:id/characters/import`（复制为项目角色图并追加定稿）。
- **实现**：`routes/characters.js`（新）+ 前端「📚 从角色库导入」/「⭐ 收藏到角色库」+ `openModal` 通用弹窗。
- **验证**：端到端冒烟——从 E02 收藏 2 角色 → 新建项目导入 → 批量建 2 镜（含 `ref_image_ids` 精确引用）全通。
- **遗留**：导入时未校验库中图片 URL 可达性（见 P3-2）；系列模板的"新建项目一键套用（含角色导入）"UI 待做（见 P1-1）。

---

## P1 — 批量化与可视化（12 集 = 156 镜，编排成本必须压下去）

### 🟡 P1-1 系列模板 + 分镜批量导入（部分落地 v2.5）

- **已落地**：**分镜批量导入** `POST /api/projects/:id/shots/bulk`（append|replace + 一次性校验 + 前端 JSON 粘贴弹窗）；
  **系列模板字段**：创作模板扩展 `character_ids`（角色库引用）/`voice`/`bgm_song_id`/`naming`。
- **待做**：新建项目时"套用系列模板"的前端闭环——自动回填风格/画幅/时长/渲染配方 **+ 从角色库导入角色 + 按 naming 生成项目名**（后端字段已就位，缺 UI 编排）。
- **原问题**（保留）：每集重复"建项目 → 录 N 镜 → 配 N 条音 → 设逐镜偏移 → 选 BGM → 渲染"；
  实测 E02 漏设镜 12 的 `use_character_ref`，事后补 PATCH——批量导入已消除此类手工编排错误。

### ⬜ P1-2 项目级"制作矩阵"（一屏看清这集差什么）

- **问题**：镜头级状态（视频/配音/定稿 take/时长校验）只能靠脚本 GET 后自己拼；
  看板上也难一眼看出"这集还差几镜"。
- **建议**：项目详情加"制作矩阵"面板——行=镜头，列=视频✓ / 配音✓ / 定稿 take / `tts.duration+0.5 ≤ 镜长` /
  是否含 `<Picture 1>`，异常项标红；顶部一个"整集就绪度"（N/13）。

---

## P2 — 质检与诊断（AI 创作者的结构性补强，本两集暴露最多）

### ✅ P2-1 渲染质检面板（把诊断脚本产品化）—— 已落地 v2.5

- **落地**：`GET /api/render/jobs/{id}/inspect`（按需生成缓存：关键帧 4 张 + 音频波形图 + 音视频流时长对比
  - 客观指标 + hints 告警）；前端渲染任务卡「🔍 质检图与指标」弹窗。
- **实测**：E02 成片（#97）22 秒出结果——流差 0（音轨铺满）、亮度波动 16.9、闪烁帧占比 0。
- **意义**：此前每次质检需临时写 ffmpeg 脚本 + 手工看图；现在一键可得，且"音频流 vs 视频流"这类**曾导致尾部静音的隐性缺陷**可直接读数。
- **遗留**：指标为整片粒度（见 P2-2：镜头级指标仍待做）。

### ✅ P2-2 客观视频指标（补"AI 看不了动态"）—— 已落地 v2.5

- **落地**：`lib/video-metrics.js`（共享：`luma_mean`/`luma_std`/`flash_ratio`/`motion_mean`/`sampled_frames`）
  - 成片级（`GET /api/render/jobs/{id}/inspect`）+ **镜头级**（`GET /api/tasks/{id}/metrics`）。
- **实测**：E02 镜4（静镜）18 秒出结果——亮度 86.1 / 波动 0.3 / 闪烁 0 / 运动 0.6，与"老人捧鞋的静态镜"相符。
- **用法**：AI 先按指标筛可疑镜头（`flash_ratio` 高 = 闪烁、`motion_mean` 异常 = 抖动/幻灯片），再交人工复核少数。

---

## P3 — 细节补丁

### ✅ P3-1 `ambient_volume` 默认值 —— 已落地 v2.5

- 默认 0 → **0.2**（实测沉浸感更好且不抢旁白；0 仍可显式关闭）。

### ✅ P3-2 角色图 remote_url 的长效性 —— 已落地 v2.5

- 角色库导入前**探测 URL 可达性**（GET + Range 0-0，8s 超时）；不可达则跳过并在响应 `skipped` 中回报原因
  （避免"导入成功、提交视频时才发现拉不到图"）。

### ✅ P3-3 日文音拍数估算 —— 已落地 v2.5

- 新增 `estimateJaMoras` / `hasKana`（`services/prompts.js`，含单测）；TTS 长度校验对日文改走
  「**音拍 ≤ 秒数 × 7**」口径（中文仍为字数 × 4）。

### ✅ P3-4 逐镜 `offset_ms` 的 API —— 已落地 v2.5

- `PATCH /api/tts/{id} {offset_ms(0–3000 或 null)}`；**并修复既有 bug**：tts 行映射漏 `offset_ms`，
  导致渲染侧读不到逐镜偏移（E01/E02 的台词镜实际上一直用的全局偏移）。

---

## 环境与工程

### ⬜ E-1 `npm run dev`（watch + 优雅重启）

- 改渲染代码后必须手动 kill 进程再启动（本机 PowerShell 下 kill 还会让 AI 终端工具报 `ChildProcess.kill` 错）。
- 建议：`npm run dev`（node --watch 或 nodemon）+ 优雅退出；或提供本地重启脚本。

### ⬜ E-2 `tools/` 目录（让脚本成为可复用资产）

- AI 的创作/诊断脚本目前只能放系统临时目录，无法沉淀、无法复用（每集重写）。
- 建议：项目内 `tools/`（或 `scripts/`）承载：分镜批量导入、质检、诊断、一致性校验等脚本。

### ⬜ E-3 "运营操作" API 化

- 复制角色图（P0-2）、设 `offset_ms`（P3-4）目前在直连 SQLite 操作；建议补 API，避免绕契约。

---

## 沉淀机制（让"复用"不靠人记）

> 实战做法（每集写"制作记录"）有效，但依赖 AI 自觉。建议机制化：

### ⬜ S-1 一致性台账（单一来源）

- 内容：角色图 id ＋ 服色锚文本 ＋ 风格锚 ＋ 用语表（中日）＋ 用过的 BGM/音色 ＋ 各集时长。
- 现状：散落于 `docs/stories/幻灯屋-系列企划.md` 与 `-制作记录.md`；建议抽成独立文件
  （或 JSON / settings），所有集数引用同一份，**防跨集漂移**。

### ⬜ S-2 每集制作档案（自动草稿）

- 渲染完成时自动生成草稿：参数（风格锚/音色/BGM/渲染配方）、规格（镜数/时长/响度/偏差）、
  质检结果、封面、作品目录。人/AI 只需补"复盘与教训"。

### ⬜ S-3 开拍 checklist

- 第 N 集开机前自动列出：本集角色（图是否齐）、场景图、备选 BGM、时长预算、上集钩子承接。

---

## 附录 A：本轮实战已落地（S1E01–E02 期间）

| 项                               | 说明                                                                                      |
| -------------------------------- | ----------------------------------------------------------------------------------------- |
| ✅ BGM 尾端静音修复              | `sidechaincompress` 输出随侧链截断 → 侧链 `apad` 补静音到片长（137.7s 片音频曾仅 128.4s） |
| ✅ BGM 起始偏移 `bgm_start_ms`   | 跳过音源开头静音 padding / 爆音（"咯噔"根因：音源 0.25s 处 -0.2dB 爆音）                  |
| ✅ 镜头原声混入 `ambient_volume` | 可选低音量混入 AI 环境声（默认 0=剥离）；归一化保留音轨 + 终混重构                        |
| ✅ 片头/片尾卡主题化 + 署名      | 场景图背景 + 主/副标题 + `creator` 署名；片头时长 2.8s→3.8s                               |
| ✅ 双语硬字幕验证                | 中文脚本（上）+ 日文配音（下），跨 26 镜验证通过                                          |
| ✅ 跨集角色图复用（临时方案）    | DB 复制 `project_images`（待 P0-2 正规化）                                                |

## 附录 B：AI 创作者能力边界备忘（已写入制作记录，此处汇总）

1. **能看图，不能看动态**：静态质量（构图/色彩/一致性）可自评；运动质量（抖动/闪烁/动作）需人工复核；
   客观指标（P2-2）可部分补强。
2. **判型 > 调措辞**：反复失败先判"模型弱项 / prompt 放大 / 锚缺失"，判错型会白重拍。
3. **改构图 > 改措辞**：可数物件、画面内文字、特定时代感＋微情绪——措辞难根治，**换构图/背景虚化/剪影**最有效。
4. **用形式换容错**：赛璐璐/动画 + 单元剧 + 独白叙事 + 少对白，是当前模型下的高胜率组合。
